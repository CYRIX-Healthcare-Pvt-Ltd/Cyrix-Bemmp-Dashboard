node_modules/
dist/

# Machine-specific tool permissions, not project config
.claude/settings.local.json

# Generated by `npm run build:data` — regenerate, don't commit (19 MB)
public/data/

# Source exports are large SharePoint-synced binaries
BEMMP DATA/
desktop.ini

# Real contract figures and sample values — this repository is public
BASELINE.md

# Local secrets: OpenAI key, site password. Read by scripts/serve.mjs only,
# never bundled and never served to the browser.
.env.local
# Docker reads .env; .env.example is the committed template.
.env
